services:
  tizentube:
    build: .
    container_name: tizentube
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    environment:
      - TIZENTUBE_DATA_DIR=/app/data/profiles
